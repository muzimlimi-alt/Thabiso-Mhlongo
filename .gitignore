backups/
node_modules/
.env

# Installed Claude Code skill packages (vendored, reference data - not project source)
.claude/skills/
.agents/skills/

# Runtime databases / session stores (regenerated at runtime, not source)
database.sqlite
database.sqlite-shm
database.sqlite-wal
sessions.sqlite
*.sqlite-shm
*.sqlite-wal
sessions.sqlite-*

# Generated PDFs (invoices/quotes/contracts/attachments) — Phase 3 moved new writes to an
# external DOCS_PATH, but this stays here too in case DOCS_PATH is ever pointed back in-repo.
docs/

# Local scratch / verification helpers and logs
scratch/
*.log
puppeteer_verify.js
verify_login_logs.js

# integration test throwaway DB + docs/uploads (Phase 3 DOCS_PATH/UPLOADS_PATH overrides)
test/.test.sqlite
test/.test.sqlite-*
test/test-docs/
test/test-uploads/

# Temporary and test files directory
TBC/

# IDE Workspcace
Thabiso Mhlongo Official Website.code-workspace
